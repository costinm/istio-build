apiVersion: v1
kind: Service
metadata:
  name: istio-pilot-raw
  labels:
    istio: pilot
spec:
  ports:
  - port: 8080
    name: http-discovery
  - port: 8081
    name: http-apiserver
  selector:
    istio: pilot