kind: Service
apiVersion: v1
metadata:
  name: rawtest
spec:
  ports:
    - protocol: TCP
      port: 9000

---

kind: Endpoints
apiVersion: v1
metadata:
  name: rawtest
subsets:
  - addresses:
      - ip: {{.Values.IP}}
    ports:
      - port: 9000
